const { test, expect } = require("@jest/globals");
const getTop100WordSequences = require("../getTop100WordSequences");

test("Return sequences from single file", () => {
    expect(8).toBe(7);

});

test("Return sequences from two files", () => {
    expect(8).toBe(7);
});

